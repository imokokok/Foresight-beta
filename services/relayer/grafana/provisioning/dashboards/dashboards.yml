# Grafana Dashboard 配置

apiVersion: 1

providers:
  - name: "Foresight Dashboards"
    orgId: 1
    folder: "Foresight"
    folderUid: "foresight"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
